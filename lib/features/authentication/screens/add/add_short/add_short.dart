
import 'package:flutter/material.dart';


class AddShort extends StatelessWidget {
  const AddShort({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Short')),
      body: const Center(child: Text('Add Short Screen')),
    );
  }
}